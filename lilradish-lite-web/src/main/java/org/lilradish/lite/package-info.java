/**
 * The application's own root, holding what is true of the whole of it rather than of any one part:
 * where it starts, and how it renders itself in a log line. Both answer for the whole application,
 * so neither belongs to a feature and neither belongs to the shape a page asks in. A component scan
 * begins here, which is why nothing beneath has to be named for it to be reachable.
 */
@NullMarked
package org.lilradish.lite;

import org.jspecify.annotations.NullMarked;
