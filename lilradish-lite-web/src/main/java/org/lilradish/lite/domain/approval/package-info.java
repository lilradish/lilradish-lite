/**
 * What happened at the gates a run passed through, never what one of them demands: a gate is part of
 * what a workflow declares, and is declared where the rest of that definition is. A decision names
 * the attempt it was made about, so nothing here means anything apart from the attempt it belongs
 * to.
 */
@NullMarked
package org.lilradish.lite.domain.approval;

import org.jspecify.annotations.NullMarked;
