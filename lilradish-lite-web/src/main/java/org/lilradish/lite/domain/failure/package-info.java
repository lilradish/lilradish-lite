/**
 * Refusals the domain raises for a caller. Nothing here knows about HTTP: a category is the
 * transport-neutral half, and mapping it to a status belongs to whichever outlet answers.
 */
@NullMarked
package org.lilradish.lite.domain.failure;

import org.jspecify.annotations.NullMarked;
