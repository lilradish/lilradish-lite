package org.lilradish.lite.domain.workflow;

/**
 * Filters candidates rather than ranking them; an empty requirement set admits any model, text
 * being every model's baseline.
 */
public enum Modality {
    VISION
}
